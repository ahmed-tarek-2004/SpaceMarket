export interface ApiServiceItem {
  id: string;            
  categoryId?: string;
  title: string;
  description?: string;
  categoryName?: string;
  providerName?: string;
  price?: number;
  imagesUrl?: string;
}